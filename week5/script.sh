#!/bin/bash

if [ -z "$1" && -z "$2" ]; then
	echo "Error, Failed to copy, Input the parameters"
elif
	if [ -d $2 ]; then
		echo "The destination given does not exist, creating directory"
		mkdir 
	else

	fi
fi
 
